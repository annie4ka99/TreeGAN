 private void paintExtSqRect(int xmin, int ymin, int extWidth, int extHeight) { int pixXmin = xOffset + xmin * skin.graphicWidth; int pixYmin = yOffset + ymin * skin.graphicHeight; int pixWidth = extWidth * skin.graphicWidth; int pixHeight = extHeight * skin.graphicHeight; if (pixWidth > 0 || pixHeight > 0) { repaint(pixXmin, pixYmin, pixWidth, pixHeight); paintMovesPushes(); } } 